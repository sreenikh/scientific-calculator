# Pending manual assets — GraphN² devlog

One asset remains pending. The Casio composite image has been generated
programmatically and wired into the article.

---

## 1. ~~Physical calculator photo~~ — DONE

The composite `fx991es-and-graphn2.jpg` was generated by `scripts/fix-media.mjs`
using the user-provided digital photo of the fx-991ES PLUS (2nd edition)
side-by-side with a GraphN² screenshot. Wired into Section 11 of the devlog
and Part 3.

---

## 2. First mockup vs. finished app

- [ ] Check out commit `3636c9b` ("Initial commit: scientific calculator with
      MathLive + math.js") and run `npm run dev` — this is the earliest
      committed React app (Phase 1). The static HTML mockup was never
      committed to git, so this is the best available "before" state.
- [ ] Screenshot it in the same calculator mode as the current finished app
- [ ] Combine into one side-by-side image

**Goes in:** Section 5 ("How This Actually Got Built") before/after
placeholder, which appears in both the main devlog and **Part 1**.

**Save as:** `docs/journal/media/mockup-vs-finished.png`

Once created:
- Drop in `docs/journal/media/`
- Replace the two remaining `<!-- PENDING MANUAL ASSET -->` comments in
  `docs/journal/graphn2-article.md` (Section 5) and
  `docs/journal/medium/graphn2-part1-medium.md` with real image embeds
